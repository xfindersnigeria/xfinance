import React from 'react';
import { Items } from '@/components/features/user/income/items';

/**
 * Items Page - Income Module
 * Displays list of items (services and goods) for invoicing
 * Provides functionality to create, read, update, and delete items
 */
export default function ItemsPage() {
  return <Items />;
}
